# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing a sample home page
GET     /                           controllers.HomeController.index
POST    /                           controllers.HomeController.loginSubmit
# An example controller showing how to use dependency injection
GET     /count                      controllers.CountController.count
# An example controller showing how to write asynchronous code
GET     /message                    controllers.AsyncController.message

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)

#In this method we can add person to the database
#POST    /addPerson                  controllers.HomeController.addPerson

GET    /add_semester                controllers.HomeController.addSemesterInfoFile
POST   /add_semester                controllers.HomeController.getSemesterFile
GET    /home                        controllers.HomeController.home